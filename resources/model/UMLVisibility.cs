/// <summary>
/// UML member visibility. <see cref="Unknown"/> is what a member gets when the
/// source code leaves the visibility out.
/// </summary>
public enum UMLVisibility
{
	Unknown,
	Public,
	Private,
	Package,
	Protected,
}
